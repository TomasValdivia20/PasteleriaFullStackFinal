# ===================================================================
# CONFIGURACION DE PRODUCCION - RAILWAY + SUPABASE FREE TIER
# ===================================================================
# 🔴 OPTIMIZADO PARA: Supabase Free Tier (20 conexiones max)
# Este perfil se activa con SPRING_PROFILES_ACTIVE=production en Railway

# ===================================================================
# HIKARI CONNECTION POOL - ULTRA OPTIMIZADO PARA SUPABASE FREE
# ===================================================================
# 🔴 CAMBIO CRÍTICO: 1 conexión máxima (reducido de 3)
# Razón: Supabase Free Tier tiene 20 conexiones TOTALES
# Con crash loops Railway, 3 conexiones saturan el pool rápidamente
spring.datasource.hikari.maximum-pool-size=1

# 🔴 CAMBIO: 0 conexiones idle mínimas (reducido de 1)
# Razón: No mantener conexión ociosa cuando no hay tráfico
spring.datasource.hikari.minimum-idle=0

# 🔴 CAMBIO: Timeout de conexión agresivo (5 segundos)
# Razón: Falla rápido si Supabase está saturado
spring.datasource.hikari.connection-timeout=5000

# 🔴 CAMBIO CRÍTICO: Idle timeout 30 segundos (reducido de 10 minutos)
# Razón: Cierra conexiones inactivas rápidamente
# Evita acumulación de conexiones zombies
spring.datasource.hikari.idle-timeout=30000

# 🔴 CAMBIO CRÍTICO: Vida máxima 60 segundos (reducido de 20 minutos)
# Razón: Recicla conexión cada minuto
# Previene conexiones zombies de larga duración
spring.datasource.hikari.max-lifetime=60000

# 🔴 Leak detection: 10 segundos (reducido de 15)
# Razón: Detectar leaks de conexión más rápido
spring.datasource.hikari.leak-detection-threshold=10000

# ===================================================================
# FLYWAY - DESHABILITADO EN PRODUCCION
# ===================================================================
# 🔴 CRÍTICO: Flyway deshabilitado
# Razón: Migraciones YA aplicadas (V7 exitoso)
# Flyway consume 1 conexión al inicio que no se libera en crash loops
spring.flyway.enabled=false

# ===================================================================
# JPA - SIN LOGGING EN PRODUCCION
# ===================================================================
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===================================================================
# LOGGING - OPTIMIZADO PARA RAILWAY
# ===================================================================
logging.level.root=INFO
logging.level.com.milsabores.backend=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=WARN
logging.level.com.zaxxer.hikari=DEBUG
# 🔴 Debug HikariCP para monitorear conexiones
logging.level.org.springframework.jdbc=INFO
logging.level.org.flywaydb=ERROR
# Silenciar Flyway (ya deshabilitado)

# ===================================================================
# ERROR HANDLING
# ===================================================================
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never
server.error.include-exception=false

# ===================================================================
# JWT SECURITY
# ===================================================================
# Configurado via variables de entorno en Railway:
# - JWT_SECRET
# - JWT_EXPIRATION
